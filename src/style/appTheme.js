import { createTheme } from "@mui/material";

const appTheme = createTheme({
  palette: {
    primary: {
      main: "#caf0f9",
    },
    secondary: {
      main: "#90e0ef",
    },
  },
});
export default appTheme;
