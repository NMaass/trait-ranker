import { createTheme } from "@mui/material";

const appTheme = createTheme({
  palette: {
    primary: {
      main: "#caf0f9",
    },
    secondary: {
      main: "#90e0ef",
    },
    warning: {
      main: "#fa4d4d",
    },
  },
});
export default appTheme;
